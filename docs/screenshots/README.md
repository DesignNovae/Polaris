# Product screenshot notes

The root README leads with the current ASL interpreter and lesson library. Screenshots document actual application states; they are not generated interface mockups.

## Captures from 7 September 2026

| File | Surface | Capture conditions |
| --- | --- | --- |
| `asl-learning.jpg` | Video Learning with the 3D ASL interpreter | Signed-in test account, real Khan Academy SAT lesson, local SignSparK worker; paused after generation. |
| `learning-library.jpg` | Lesson catalog | Signed-in test account; real catalog metadata and YouTube thumbnails. |
| `learning-paths.jpg` | Structured IELTS paths | Signed-in test account; curated path descriptions and progress controls. |
| `exam-lab-current.jpg` | Exam Lab | Signed-in test account; current mock-exam catalog. |

The account name visible in these captures is a test identity. Do not replace these with screenshots showing a real student's email, private profile, conversation, or credentials. Public video titles, frames, thumbnails, and source attribution belong to their respective publishers.

## Reproduce the current captures

1. Configure the web application and run `npm run dev`.
2. Follow [the signing setup](../SIGN_LANGUAGE_PRODUCTION.md), then run `npm run signing:worker` for the ASL capture.
3. Sign in with a test account and open Action Lab → Video Learning.
4. Select SAT → Math → Solving linear equations. Enable ASL, wait for generation, play the source, and pause with the figure visible. Keep the source attribution and research-status label visible.
5. Close the player and capture the library filters/cards. Open the IELTS learning paths lower in the library for the path capture.
6. Open Action Lab → Exam Lab and capture the loaded catalog.
7. Inspect every image before committing: check legibility, loading/error states, personal information, and agreement with its README caption.

These authenticated captures are manual and are not generated by `npm run screenshots`. That script captures its fixed public/demo targets, including the older roadmap, Strategist, university, and Action Lab screens. See [the script](../../scripts/screenshots.mjs) for its target list.

ASL here demonstrates local model inference and synchronized rendering. It does not demonstrate cloud GPU deployment or establish the linguistic accuracy of generated signing.
